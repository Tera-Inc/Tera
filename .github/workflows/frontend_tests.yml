name: Run Frontend Tests

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
    paths:
      - 'tera/frontend/**'
      - 'devops/**'
  pull_request:
    branches:
      - main
    paths:
      - 'tera/frontend/**'
      - 'devops/**'

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ./tera/frontend
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'yarn'
        cache-dependency-path: tera/frontend/yarn.lock

    - name: Create .env file
      run: cp .env.dev .env

    - name: Install dependencies
      run: yarn install --frozen-lockfile

    - name: Run tests
      run: yarn test:run

    - name: Run lint
      run: yarn lint

    - name: Build frontend
      run: yarn build

    - name: Generate bundle size report
      run: yarn bundle:size

    - name: Upload bundle size report
      uses: actions/upload-artifact@v4
      with:
        name: frontend-bundle-size-report
        path: tera/frontend/bundle-size-report.json
